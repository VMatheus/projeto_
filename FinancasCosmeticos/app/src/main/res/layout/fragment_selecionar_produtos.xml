<LinearLayout
    android:orientation="vertical" xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:id="@+id/linear_selecionar_produto"
    tools:context="com.tecnoia.matheus.financascosmeticos.supervisor.fragments.SelecionarProdutos">

    <android.support.v7.widget.Toolbar

        app:theme="@style/ToolbarColoredBackArrow"
        app:popupTheme="@style/AppTheme"
        app:subtitleTextColor="@android:color/white"
        app:titleTextColor="@android:color/white"
        android:id="@+id/toolbar_selecionar_produtos"
        android:layout_width="match_parent"
        android:layout_height="?attr/actionBarSize"
        android:background="?attr/colorPrimary"
        android:elevation="4dp"
        />
    <!-- TODO: Update blank fragment layout -->
    <ListView
        android:id="@+id/list_view_selecionar_produtos"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_margin="15dp">

    </ListView>

    <android.support.design.widget.FloatingActionButton
        android:id="@+id/floating_button_selecionar_produtos"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_alignParentEnd="true"
        android:layout_gravity="bottom|end"
        android:layout_marginBottom="18dp"
        android:layout_marginEnd="14dp"
        android:layout_marginLeft="15dp"
        android:layout_marginRight="15dp"
        android:tint="@android:color/white"
        app:fabSize="normal"
        app:srcCompat="@drawable/ic_action_itens_selecionados"
    />
</LinearLayout>
